# API Configuration
# Configure the backend API endpoint based on your environment

# Development environment (local development)
# REACT_APP_API_URL=http://localhost:8000/api

# Production environment (deployed application)
# REACT_APP_API_URL=https://api.yourdomain.com/api

# Staging environment (staging server)
# REACT_APP_API_URL=https://staging-api.yourdomain.com/api

# NODE_ENV is automatically set by the build tool:
# - "development" for npm start
# - "production" for npm run build
# If REACT_APP_API_URL is not set, NODE_ENV defaults are used:
# - development → http://localhost:8000/api
# - production → https://api.yourdomain.com/api (requires explicit override)

# Note: REACT_APP_API_URL overrides NODE_ENV defaults
# This allows per-deployment configuration without rebuilding
